# Zephra 0.1.0 — build 20260906

Built on 2026-09-06 from application source at
`95dac0ae073c2b0fc0e1f7ce0ec7e6cc5b4d6b90` (website-only deployment work was in
progress). Apple Silicon, macOS 15+, Developer ID Application: James Brink.

**Superseded, and the file is gone.** This build's DMG was removed from the
releases bucket on 2026-09-08 along with every other superseded build; the
bucket has no versioning, so the bytes are not recoverable. What is kept is this
record and the source it was built from, which is the point of writing one.

The current build is always
https://zephra-assets.urandom.io/releases/Zephra-latest.dmg, with
`releases/latest.json` beside it naming which build that is and its digest.

SHA-256 of the build described here, for the record:
`5d2a498613c9290b65aa09c1827fea52592e60027b681eade9d538a82cee9e0a`

Validation performed on the file downloaded from that public HTTPS URL:

- SHA-256 identical to the local notarized installer.
- DMG signature valid; Apple ticket stapled and validated.
- Mounted app's deep/strict signature valid; app ticket stapled and validated.
- Gatekeeper accepted the mounted app as Notarized Developer ID.
- Mounted volume icon equals the app's AppIcon.icns; custom-icon flag present.
- Downloaded app icon also equals the signed local build's icon.
- Finder visibly renders the selected Zephyr icon on the downloaded app.
- Applications installation shortcut points to `/Applications`.
- HTTP 200 with DMG content type and attachment filename; link tested on AWS page.

The mounted volume and app branding are inside the DMG and survive HTTP download.
The outer DMG file's Finder custom icon is a resource fork and is not preserved by
ordinary HTTP; Finder may show its standard disk-image icon before mounting.

Local evidence (ignored build outputs): `build/production-release.log`,
`build/production-upload.log`, `build/download-verification/finder-icon.png`.
