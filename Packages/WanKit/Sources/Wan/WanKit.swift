/// Wan 2.2 TI2V-5B, the video model FastWan2.2-TI2V-5B was distilled from, on MLX.
///
/// Written from Apache-2.0 `diffusers` and `transformers`, pinned by dumped fixtures; nothing
/// copied from `Wan-Video/Wan2.2` or from any other port. Video only, three distilled steps,
/// no guidance; a picture may be held as the clip's first frame.
public enum WanKit {
    /// The frames per second the model was trained to make.
    public static let frameRate = 24.0
}
